info:
  name: Logout
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrlV1}}/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
