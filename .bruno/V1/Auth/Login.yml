info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrlV1}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "test@example.com",
        "password": "password"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
